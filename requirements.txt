# Dependencies grouped by project step.
# We install step by step on purpose -- so the role of each library stays clear.

# --- Step 1: Setup ---
pydantic-settings   # Load & validate configuration from .env in a type-safe way
qdrant-client       # Python client for the Qdrant vector database

# --- Step 2: Data pipeline ---
httpx               # HTTP client used to call the TMDb API

# --- Step 3: Embeddings & indexing ---
google-genai        # Official SDK for the Gemini embedding API

# --- Dev tooling (not needed at runtime) ---
pytest              # Unit tests for the pure data-transformation logic
